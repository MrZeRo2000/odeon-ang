import { ProcessingStatusNamePipe } from './processing-status-name.pipe';

describe('ProcessingStatusNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ProcessingStatusNamePipe();
    expect(pipe).toBeTruthy();
  });
});
