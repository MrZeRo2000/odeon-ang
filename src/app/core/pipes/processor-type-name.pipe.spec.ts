import { ProcessorTypeNamePipe } from './processor-type-name.pipe';

describe('ProcessorTypeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ProcessorTypeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
